export interface IWeeklyStats {
    total_distance: string,
    total_price: string
}

export interface IMonthlyStats {
    day: string,
    total_distance: string,
    avg_ride: string,
    avg_price: string
}